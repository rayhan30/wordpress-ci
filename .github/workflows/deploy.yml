name: Publish Website
on:
  push:
    branches:
      - main

jobs:
  web-deploy:
    name: 🚀 Deploy Website Every Commit
    runs-on: ubuntu-latest
    steps:
      - name: 🚚 Get Latest Code
        uses: actions/checkout@v3

      - name: 📂 Sync Files
        uses: SamKirkland/web-deploy@v1
        with:
          target-server: staging.abu-rayhan.me
          remote-user: u393900437
          private-ssh-key: ${{ secrets.SSH_KEY }}
          ssh-port: 65002
          source-path: wp-content/themes/twentytwentyfive/
          destination-path: /home/u393900437/domains/abu-rayhan.me/public_html/staging/wp-content/themes/twentytwentyfive/
